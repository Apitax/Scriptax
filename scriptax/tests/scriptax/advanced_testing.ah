script construct(param = "worked")
{
    worked = "test" + " " + param;
}

script get_test()
{
    return test;
}

static script day(param = "good")
{
    return param + " " + "night";
}

script get_advanced_test()
{
    return get_test() + " " + self.get_test_static() + " " + test + ".";
}


test = "worked";

worked = null;
